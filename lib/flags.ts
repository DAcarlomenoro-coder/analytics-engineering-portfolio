// Feature flags for sections that are built and ready but not yet
// recruiter-facing. Flip back to `true` (and remove the /blog redirect
// below it depends on) once the first real article is published — nothing
// else needs to change; nav, footer, and the blog page all read this flag.

export const BLOG_ENABLED = false;
